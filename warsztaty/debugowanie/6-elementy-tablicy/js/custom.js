/*
Wyświetl piąty i szosty element tablicy
*/

'use strict';

var tablica = [9, 88, 7, 66, 5, 44, 3, 22, 1];

tablica.forEach( function(element, index) {
    
    if (index == 4) {
        console.log(element);
    }
    
    if (index == 5) {
        console.log(element);
    }
    
});
